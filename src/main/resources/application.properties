spring.application.name=ChatAppBackend
spring.config.import=optional:file:.env
# database
spring.datasource.url=jdbc:mysql://localhost:3308/chatapp?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=Vlvlvl2707*
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.sql.init.mode=always
spring.jpa.defer-datasource-initialization=true

#jwt
jwt.secret-key=very-long-super-secret-key-for-jwt-authentication-12345
jwt.access-token-time=100
jwt.refresh-token-time=600
server.port=8082
#admin account
admin.username=admin
admin.password=admin123
admin.password=admin

# Email Config
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${GMAIL_USERNAME}
spring.mail.password=${GMAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Disable mail health check
management.health.mail.enabled=false

# Disable email health check
management.health.mail.enabled=false
